---
slug: data-engineering-hero-agent-resolution-and-auto-resolve
title: The Hero Agent - Resolution, Suggested Fixes, and Auto-Resolve
meta_title: Building a Data Engineering Hero Agent - Resolution and Auto-Resolve
description: How the DE hero agent tracks failure resolution end to end - a status model with cascading resolution, an AI-suggested resolution shown next to each failure, and auto-resolving failures once a rerun succeeds.
date: 2026-07-07
writing_date: 2026-07-07
image: /images/blog/9999-de-hero-agent-resolution-and-auto-resolve.jpg
category: DE
tags: [DE, Python, FastAPI, AI, Best Practices]
draft: true
---

## Motivation

<!-- Recap: stage 1 = report, previous post = summarize/classify. This post closes the
loop on a failure's lifecycle - status, resolution, and eventually auto-resolve - without
yet needing Slack buttons (that's still stage 2's interactive piece). -->

## The resolution status model

<!-- Statuses/categories on a failure event. Why resolution is modeled the way it is,
cascading resolution across related failures (#37, #40). -->

## Surfacing the suggested resolution

<!-- The suggested-resolution pill in the UI, styled like the resolution pill (#44).
How it relates to the Bedrock-suggested category from the previous post. -->

## Auto-resolving via a success endpoint

<!-- #45/#46: a success endpoint that matches decoded metadata["params"] to auto-resolve
the original failure when a rerun succeeds. Why matching on decoded params, not raw
metadata, was the actual fix. -->

## The failures summary page

<!-- Maybe fold in here rather than a separate post: filters, clickable cards, pagination,
resolution breakdown replacing retryable stat cards (#36, #40, #41). -->

## Where this leaves us

<!-- Recap. What's still manual (Slack actions) vs. what's now automatic (auto-resolve). -->
